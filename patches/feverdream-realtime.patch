diff --git a/source/backend/scene/scene.cpp b/source/backend/scene/scene.cpp
index 3aa48ec..109814f 100644
--- a/source/backend/scene/scene.cpp
+++ b/source/backend/scene/scene.cpp
@@ -688,7 +688,14 @@ void Scene::ParserControlThread()
 		if(stopRequsted == false)
 		{
 			boost::thread::yield();
-			Delay(10);
+			// Feverdream: was Delay(10), then Delay(1). At ~90 fps even a 1ms
+			// poll is ~9% of the frame; 0.2ms keeps the driver responsive
+			// without busy-spinning. POSIX-only fast path; others keep Delay.
+#if defined(__unix__) || defined(__APPLE__)
+			{ struct timespec fdts = {0, 200000}; nanosleep(&fdts, NULL); }
+#else
+			Delay(1);
+#endif
 		}
 	}
 }
diff --git a/source/backend/scene/view.cpp b/source/backend/scene/view.cpp
index 962a089..3b9c11e 100644
--- a/source/backend/scene/view.cpp
+++ b/source/backend/scene/view.cpp
@@ -681,6 +681,13 @@ void View::StartRender(POVMS_Object& renderOptions)
 	bool highReproducibility = false;
 	shared_ptr<std::set<unsigned int> > blockskiplist(new std::set<unsigned int>());
 
+#if defined(__unix__) || defined(__APPLE__)
+	if (getenv("FD_TRACE_STATES")) {
+		struct timespec _ts; clock_gettime(CLOCK_MONOTONIC, &_ts);
+		fprintf(stderr, "  [view  %ld.%06ld] StartRender entry\n", (long)_ts.tv_sec, _ts.tv_nsec/1000);
+	}
+#endif
+
 	if(renderControlThread == NULL)
 		renderControlThread = Task::NewBoostThread(boost::bind(&View::RenderControlThread, this), POV_THREAD_STACK_SIZE);
 
@@ -1401,7 +1408,14 @@ void View::RenderControlThread()
 		if(stopRequsted == false)
 		{
 			boost::thread::yield();
-			Delay(50);
+			// Feverdream: was Delay(50), then Delay(1). At ~90 fps even a 1ms
+			// poll is ~9% of the frame; 0.2ms keeps the driver responsive
+			// without busy-spinning. POSIX-only fast path; others keep Delay.
+#if defined(__unix__) || defined(__APPLE__)
+			{ struct timespec fdts = {0, 200000}; nanosleep(&fdts, NULL); }
+#else
+			Delay(1);
+#endif
 		}
 	}
 }
diff --git a/source/backend/support/randomsequences.cpp b/source/backend/support/randomsequences.cpp
index 55f250d..735689f 100644
--- a/source/backend/support/randomsequences.cpp
+++ b/source/backend/support/randomsequences.cpp
@@ -107,8 +107,28 @@ DBL POV_rand(unsigned int& next_rand)
  *  Legacy Code
  *********************************************************************************/
 
+// Feverdream: the mt19937 generators below are DEFAULT-SEEDED, so RandomInts/
+// RandomDoubles are pure functions of (minval, maxval, count) — every call with
+// the same arguments regenerates the identical sequence. TracePixel and
+// RadiosityFunction constructors call these per TraceTask, i.e. per render
+// thread PER FRAME in the resident engine: measured 33% of total daemon CPU
+// (perf, empty scene). Memoize the sequences — same key returns the same
+// values, behavior is bit-identical, generation cost is paid once.
+
 vector<int> RandomInts(int minval, int maxval, size_t count)
 {
+	typedef std::map<std::pair<std::pair<int, int>, size_t>, vector<int> > Cache;
+	static Cache cache;
+	static boost::mutex cacheMutex;
+	Cache::key_type key(std::make_pair(minval, maxval), count);
+
+	{
+		boost::mutex::scoped_lock lock(cacheMutex);
+		Cache::const_iterator it = cache.find(key);
+		if(it != cache.end())
+			return it->second;
+	}
+
 	mt19937 generator;
 	uniform_int<int> distribution(minval, maxval);
 	variate_generator<mt19937, uniform_int<int> > sequence(generator, distribution);
@@ -117,22 +137,56 @@ vector<int> RandomInts(int minval, int maxval, size_t count)
 	for(size_t i = 0; i < count; i++)
 		rands[i] = sequence();
 
+	{
+		boost::mutex::scoped_lock lock(cacheMutex);
+		if(cache.size() < 64)
+			cache[key] = rands;
+	}
+
 	return rands;
 }
 
-vector<double> RandomDoubles(double minval, double maxval, size_t count)
+// shared-ownership variant: RandomDoubleSequence holds the cached vector by
+// reference, so constructing a sequence costs a map lookup, not a 32KB copy
+static shared_ptr<vector<double> const> FdCachedRandomDoubles(double minval, double maxval, size_t count)
 {
+	typedef std::map<std::pair<std::pair<double, double>, size_t>, shared_ptr<vector<double> const> > Cache;
+	static Cache cache;
+	static boost::mutex cacheMutex;
+	Cache::key_type key(std::make_pair(minval, maxval), count);
+
+	{
+		boost::mutex::scoped_lock lock(cacheMutex);
+		Cache::const_iterator it = cache.find(key);
+		if(it != cache.end())
+			return it->second;
+	}
+
 	mt19937 generator;
 	uniform_real<double> distribution(minval, maxval);
 	variate_generator<mt19937, uniform_real<double> > sequence(generator, distribution);
-	vector<double> rands(count);
+	shared_ptr<vector<double> > rands(new vector<double>(count));
 
 	for(size_t i = 0; i < count; i++)
-		rands[i] = sequence();
+		(*rands)[i] = sequence();
+
+	{
+		// cap the memo (tri-brain: a long-lived daemon rendering arbitrary
+		// scenes must not grow RSS without bound). Distinct keys are scene
+		// constants in practice, so 64 is generous; beyond it, skip caching.
+		boost::mutex::scoped_lock lock(cacheMutex);
+		if(cache.size() < 64)
+			cache[key] = rands;
+	}
 
 	return rands;
 }
 
+vector<double> RandomDoubles(double minval, double maxval, size_t count)
+{
+	return *FdCachedRandomDoubles(minval, maxval, count);
+}
+
 RandomIntSequence::RandomIntSequence(int minval, int maxval, size_t count) :
 	values(RandomInts(minval, maxval, count))
 {
@@ -172,7 +226,7 @@ void RandomIntSequence::Generator::SetSeed(size_t seedindex)
 }
 
 RandomDoubleSequence::RandomDoubleSequence(double minval, double maxval, size_t count) :
-	values(RandomDoubles(minval, maxval, count))
+	values(FdCachedRandomDoubles(minval, maxval, count))
 {
 }
 
@@ -184,13 +238,13 @@ RandomDoubleSequence::Generator::Generator(RandomDoubleSequence *seq, size_t see
 
 double RandomDoubleSequence::operator()(size_t seedindex)
 {
-	seedindex = seedindex % values.size();
-	return values[seedindex];
+	seedindex = seedindex % values->size();
+	return (*values)[seedindex];
 }
 
 double RandomDoubleSequence::Generator::operator()()
 {
-	index = (index + 1) % sequence->values.size();
+	index = (index + 1) % sequence->values->size();
 	return (*sequence)(index);
 }
 
@@ -206,7 +260,7 @@ size_t RandomDoubleSequence::Generator::GetSeed() const
 
 void RandomDoubleSequence::Generator::SetSeed(size_t seedindex)
 {
-	index = seedindex % sequence->values.size();
+	index = seedindex % sequence->values->size();
 }
 
 
diff --git a/source/backend/support/randomsequences.h b/source/backend/support/randomsequences.h
index f0402ef..e8b4495 100644
--- a/source/backend/support/randomsequences.h
+++ b/source/backend/support/randomsequences.h
@@ -97,7 +97,12 @@ class RandomDoubleSequence
 				size_t index;
 		};
 	private:
-		vector<double> values;
+		// Feverdream: the sequence is a pure function of the ctor args
+		// (default-seeded mt19937) and was regenerated AND copied per
+		// TraceTask per frame in the resident engine. Hold a shared
+		// reference to the process-wide memoized vector instead — zero
+		// copies, bit-identical values.
+		shared_ptr<vector<double> const> values;
 };
 
 
diff --git a/source/backend/support/task.cpp b/source/backend/support/task.cpp
index c8168aa..2ae272f 100644
--- a/source/backend/support/task.cpp
+++ b/source/backend/support/task.cpp
@@ -46,6 +46,9 @@
 // this must be the last file included
 #include "base/povdebug.h"
 
+#include <deque>
+#include <vector>
+
 namespace pov
 {
 
@@ -53,6 +56,111 @@ using namespace pov_base;
 
 IMPLEMENT_THREAD_LOCAL_PTR(Task::TaskData, Task::taskDataPtr, TaskDataCleanup);
 
+// ---------------------------------------------------------------------------
+// Feverdream persistent worker pool (the .kkrieger lesson, FINDINGS.md).
+//
+// Stock POV spawns a fresh boost::thread for EVERY task — per frame in the
+// resident engine that's 1 parser task + N trace tasks created and joined,
+// ~3 ms of pure thread churn at 8 threads (measured, GAME_ENGINE.md research:
+// persistent pools fed per-frame are standard real-time raytracing practice).
+//
+// Workers are created on demand (never more than the max concurrent task
+// count), run TaskThread jobs in a loop, and live for the process lifetime.
+// Each job still does its own POVMS_OpenContext / POV_SYS_THREAD_STARTUP /
+// CLEANUP inside TaskThread, so per-task semantics are unchanged — only the
+// OS thread create/join is amortized. Set FD_NO_TASK_POOL=1 to fall back to
+// stock thread-per-task behavior.
+// ---------------------------------------------------------------------------
+namespace
+{
+
+struct FdJob
+{
+	boost::function0<void> fn;
+	bool done;
+	boost::mutex m;
+	boost::condition_variable cv;
+	FdJob(const boost::function0<void>& f) : fn(f), done(false) { }
+};
+
+class FdTaskPool
+{
+	public:
+		static FdTaskPool& Instance()
+		{
+			// intentionally leaked: workers block on the CV for the process
+			// lifetime, so a static-destruction teardown would race them
+			// (tri-brain review finding). The OS reclaims at exit.
+			static FdTaskPool *pool = new FdTaskPool();
+			return *pool;
+		}
+
+		FdJob *Submit(const boost::function0<void>& fn)
+		{
+			FdJob *job = new FdJob(fn);
+			boost::mutex::scoped_lock lock(m);
+			// a worker is spawned only when none are idle; pool size converges
+			// on the max concurrent task count and never shrinks
+			if(idle == 0)
+				workers.push_back(Task::NewBoostThread(boost::bind(&FdTaskPool::Worker, this), POV_THREAD_STACK_SIZE));
+			queue.push_back(job);
+			cv.notify_one();
+			return job;
+		}
+
+		// join-equivalent: blocks until the job has fully finished (TaskThread
+		// returned, completion() called), then frees it
+		void Wait(FdJob *job)
+		{
+			{
+				boost::mutex::scoped_lock lock(job->m);
+				while(job->done == false)
+					job->cv.wait(lock);
+			}
+			delete job;
+		}
+
+	private:
+		boost::mutex m;
+		boost::condition_variable cv;
+		std::deque<FdJob *> queue;
+		std::vector<boost::thread *> workers;   // live for the process lifetime
+		int idle;
+
+		FdTaskPool() : idle(0) { }
+
+		void Worker()
+		{
+			for(;;)
+			{
+				FdJob *job;
+				{
+					boost::mutex::scoped_lock lock(m);
+					idle++;
+					while(queue.empty() == true)
+						cv.wait(lock);
+					idle--;
+					job = queue.front();
+					queue.pop_front();
+				}
+				job->fn();
+				// after the unlock below the waiter may delete job — do not
+				// touch it again
+				boost::mutex::scoped_lock jlock(job->m);
+				job->done = true;
+				job->cv.notify_all();
+			}
+		}
+};
+
+bool FdPoolEnabled()
+{
+	static const bool disabled = (getenv("FD_NO_TASK_POOL") != NULL);
+	return disabled == false;
+}
+
+}
+
 Task::Task(TaskData *td, const boost::function1<void, Exception&>& f) :
 	taskData(td),
 	fatalErrorHandler(f),
@@ -64,6 +172,7 @@ Task::Task(TaskData *td, const boost::function1<void, Exception&>& f) :
 	realTime(-1),
 	cpuTime(-1),
 	taskThread(NULL),
+	poolJob(NULL),
 	povmsContext(NULL)
 {
 	if (td == NULL)
@@ -101,8 +210,13 @@ POV_LONG Task::ConsumedCPUTime() const
 
 void Task::Start(const boost::function0<void>& completion)
 {
-	if((done == false) && (taskThread == NULL))
-		taskThread = NewBoostThread(boost::bind(&Task::TaskThread, this, completion), POV_THREAD_STACK_SIZE);
+	if((done == false) && (taskThread == NULL) && (poolJob == NULL))
+	{
+		if(FdPoolEnabled() == true)
+			poolJob = FdTaskPool::Instance().Submit(boost::bind(&Task::TaskThread, this, completion));
+		else
+			taskThread = NewBoostThread(boost::bind(&Task::TaskThread, this, completion), POV_THREAD_STACK_SIZE);
+	}
 }
 
 void Task::RequestStop()
@@ -120,6 +234,12 @@ void Task::Stop()
 		delete taskThread;
 		taskThread = NULL;
 	}
+
+	if(poolJob != NULL)
+	{
+		FdTaskPool::Instance().Wait((FdJob *)poolJob);
+		poolJob = NULL;
+	}
 }
 
 void Task::Pause()
diff --git a/source/backend/support/task.h b/source/backend/support/task.h
index 2c081f7..fc0dbc7 100644
--- a/source/backend/support/task.h
+++ b/source/backend/support/task.h
@@ -68,7 +68,9 @@ class Task
 		virtual ~Task();
 
 		inline bool IsPaused() { return !done && paused; }
-		inline bool IsRunning() { return !done && !paused && !stopRequested && (taskThread != NULL); }
+		// Feverdream: a task may run on a pooled worker (poolJob) instead of a
+		// dedicated thread (taskThread); IsRunning must honor both.
+		inline bool IsRunning() { return !done && !paused && !stopRequested && ((taskThread != NULL) || (poolJob != NULL)); }
 		inline bool IsDone() { return done; }
 		inline bool Failed() { return done && (failed != kNoError); }
 
@@ -161,6 +163,9 @@ class Task
 		POV_LONG cpuTime;
 		/// task thread
 		boost::thread *taskThread;
+		/// Feverdream: opaque handle into the persistent worker pool (task.cpp);
+		/// NULL when running on a dedicated thread or not started.
+		void *poolJob;
 		/// POVMS message receiving context
 		POVMSContext povmsContext;
 
diff --git a/vfe/vfepovms.cpp b/vfe/vfepovms.cpp
index e9e7063..a01e578 100644
--- a/vfe/vfepovms.cpp
+++ b/vfe/vfepovms.cpp
@@ -187,10 +187,18 @@ void *SysQNode::Receive (int *pLen, bool Blocking)
     if (Blocking == false)
       return (NULL);
 
-    // TODO: have a shorter wait but loop, and check for system shutdown
+    // Feverdream Engine: was 50ms (50000000ns) — fine for batch, but it gates
+    // every frontend<->backend phase message, costing ~8x50ms=360ms/frame of
+    // pure sleep in a resident real-time loop. 1ms poll => ~real-time. The
+    // condition still wakes immediately on notify; this only bounds the fallback.
     boost::xtime t;
     boost::xtime_get (&t, POV_TIME_UTC);
-    t.nsec += 50000000 ;
+    t.nsec += 20000 ;    // 20µs (was 50ms, then 0.1ms). Each rendered pixel-BLOCK is a
+                         // POVMS message; the backend->frontend send doesn't reliably wake
+                         // this cv, so every block eats one full poll. Dozens of blocked
+                         // hops/frame => poll granularity IS the frame floor. Phase-2
+                         // measurement (empty scene 64x36 = 5.4ms floor) showed the
+                         // pipeline, not parse/trace, dominates — 20µs shaves it again.
     m_Event.timed_wait (lock, t);
 
     if (m_Count == 0)
diff --git a/vfe/vfesession.cpp b/vfe/vfesession.cpp
index 4e99628..03f8864 100644
--- a/vfe/vfesession.cpp
+++ b/vfe/vfesession.cpp
@@ -504,6 +504,13 @@ bool vfeSession::ProcessFrontend (void)
 
 void vfeSession::StateChanged (pov_frontend::State NewState)
 {
+#if defined(__unix__) || defined(__APPLE__)
+  if (getenv("FD_TRACE_STATES")) {
+    struct timespec _ts; clock_gettime(CLOCK_MONOTONIC, &_ts);
+    fprintf(stderr, "  [state %ld.%06ld] %d -> %d\n",
+            (long)_ts.tv_sec, _ts.tv_nsec/1000, (int)m_BackendState, (int)NewState);
+  }
+#endif
   if (NewState == kReady || NewState == kStopping)
     if (m_BackendState > kReady)
       RenderStopped () ;
